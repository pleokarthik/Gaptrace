# gaptrace — Documentation

These travel with the repository and are not gitignored.

- `ARCHITECTURE.md` — the current picture: the capture/run naming law,
  the dependency star around gaptrace-core, schema ownership, error routing.
- `design-doc.md` — full design rationale and architecture
- `scope.md` — delivery scope, acceptance criteria, build order
- `execution-flow.md` — method-by-method call trace, file:line references
  included so it stays checkable against the source
